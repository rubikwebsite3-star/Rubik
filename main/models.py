from django.db import models

# All application data has been migrated to Firebase Firestore & ImgBB.
# Local models are no longer used for property listings, gallery, or categories.
# db.sqlite3 is retained only for Django's built-in Authentication and Sessions.
